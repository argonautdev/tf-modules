# Run the following command to find gcp available images and it's families
```
command: gcloud compute images list
ex: 
  ./gcloud compute images list | grep "ubuntu"
Ref: https://cloud.google.com/compute/docs/images#gcloud
```

